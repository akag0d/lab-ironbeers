<div>
    {{#each beersFromApi}}
    <div>
        <img src="{{image_url}}" alt="{{name}}">
        <h1>{{name}}</h1>
        <p>{{description}}</p>
        <p>{{tagline}}</p>
    </div>
    {{/each}}
</div>